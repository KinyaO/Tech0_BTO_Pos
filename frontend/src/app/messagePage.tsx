import { NextPage } from 'next';

const MessagePage: NextPage = () => {
    return (
        <div>
            <h1>メッセージページ</h1>
            <p>ここに何か重要なメッセージが表示されます。</p>
        </div>
    );
};

export default MessagePage;
